---
# yaml-language-server: $schema=https://datreeio.github.io/CRDs-catalog/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: documenso
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: staging
  target:
    name: documenso-ext-secret
    template:
      data:
        # SMTP — reuses the n8n relay credentials; swap to a dedicated
        # 1Password item if Documenso should send from its own identity
        NEXT_PRIVATE_SMTP_TRANSPORT: smtp-auth
        NEXT_PRIVATE_SMTP_HOST: "{{ .n8n_smtp_host }}"
        NEXT_PRIVATE_SMTP_PORT: "{{ .n8n_smtp_port }}"
        NEXT_PRIVATE_SMTP_USERNAME: "{{ .n8n_smtp_user }}"
        NEXT_PRIVATE_SMTP_PASSWORD: "{{ .n8n_smtp_pass }}"
        NEXT_PRIVATE_SMTP_FROM_ADDRESS: "{{ .n8n_smtp_sender }}"
        NEXT_PRIVATE_SMTP_FROM_NAME: Documenso
        NEXT_PRIVATE_SMTP_SECURE: '{{ if eq .n8n_smtp_port "465" }}true{{ else }}false{{ end }}'
        # Postgres Init
        INIT_POSTGRES_SUPER_USER: postgres
        INIT_POSTGRES_SUPER_PASS: "{{ .POSTGRES_SUPER_PASS }}"
  dataFrom:
    - extract:
        key: n8n
    - extract:
        key: cloudnative-pg
---
# yaml-language-server: $schema=https://datreeio.github.io/CRDs-catalog/external-secrets.io/externalsecret_v1.json
# API token for automation (Claude / n8n) — not consumed by the app pod.
# Retrieve: kubectl get secret -n default documenso-api-token -o jsonpath='{.data.DOCUMENSO_API_TOKEN}' | base64 -d
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: documenso-api-token
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: staging
  target:
    name: documenso-api-token
    template:
      data:
        DOCUMENSO_API_TOKEN: "{{ .documenso_api_key_claude }}"
  dataFrom:
    - extract:
        key: documenso
